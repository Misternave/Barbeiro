**//Passos por fazer //*
--FRONTEND--

Ao selecionar a data ele deve criar um evento para carregar as horas disponiveis.

{

    algo parecido

    function fetchAvailableTimes(){
	$.ajax({
        url: window.location.origin+'/api/v1.0/store/'+$('.f_store').val()+'/availabletime?date='+$('#selectDate').val(),
        method: 'GET',
        dataType: 'json',
        success: function (d) {
        	var horas = d;
        	if(horas.length == 0){
        		var option = new Option('Sem horas disponíveis',0);
        		$('.f_hour').append($(option));
        		return;
        	}

        	for(i = 0 ; i < horas.length ; i++){
        		var option = new Option(horas[i],horas[i]);
        		$('.f_hour').append($(option));
        	}
        }
    });
};
}

--BACKEND--


*Prioridade 1*


Adicionar reservas ao MongoDB

Aparecer as reservas efetuadas por cada utilizador
Aparecer as reservas por dia para cada barbeiro(listagem de clientes por dia)

*Prioridade 2*



*Prioridade 3*

Configurar o .ENV com os dados (MongoDB/Port)





email e password

email: barbeironoobs@gmail.com
pass: Barbeiro2020!


---CRIAR UM DISCORD----



--FRONTEND--
Criar HTML // CSS

--History--

Mudado nomes da variaveis no barbaCon
"" nome do ficheiro JSON
Alterado Route barbeiro
Adicionado CRUD do Barbeiro
