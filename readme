**//Passos por fazer //*
--FRONTEND--
//Adicionar pagina Login e registo do utilizador
//Mudar o formato da data do DatePicker (Talvez JQUERY?)
//Mudar formato MongoDB horas pra Timestamp?//Criar CSS notificação quando não se coloca a data (Em vez do console.alert)

--BACKEND--
*prioridade 0*
-Colocar validação dos dados no FRONTEND ( express-validator) // Sanization -- https://www.youtube.com/watch?v=rBzCvbA0Dls&list=PLillGF-RfqbYRpji8t4SxUkMxfowG4Kqp&index=8
-Flash messages

Listing 22.3. Adding flash messages to the create action in usersController.js

create: (req, res, next) => {
  let userParams = getUserParams(req.body);
  User.create(userParams)
    .then(user => {
      req.flash("success", `${user.fullName}'s account created
 successfully!`);
      res.locals.redirect = "/users";
      res.locals.user = user;
      next();
    })
    .catch(error => {
      console.log(`Error saving user: ${error.message}`);
      res.locals.redirect = "/users/new";
      req.flash(
        "error",
        `Failed to create user account because:  ${error.message}.`
      );
      next();
    });
},
-Colocar a funcionar registo com o Login


*Prioridade 1*
Colocar o Remember me no Login
Corrigir carregamento de horas(só pode aparecer horas que sejam >= a hora atual)
Refatoring processo de password /Login/Registo (https://www.youtube.com/watch?v=SnoAwLP1a-0)
Criar relacão reserva <-> Utilizador
Utilizador logado por fazer reservas

*Prioridade 2*
Estudar CORDS / Estudar HELMET 
Aparecer as reservas efetuadas no login do utilizador


*Prioridade 3*

Sistema de pontos nos utilizadores registados
Configurar o .ENV com os dados (MongoDB/Port)

**For future study**
->Boostrap;
->Postman;
->SinglePage vanilla JS



email e password

email: barbeironoobs@gmail.com
pass: Barbeiro2020!


---CRIAR UM DISCORD----



--FRONTEND--
Criar HTML // CSS

